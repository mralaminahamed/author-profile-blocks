/**
 * Shared inspector components for Author Profile Blocks.
 *
 * Each component below is consumed by the per-block inspector panels
 * (Content / Style / Layout / Advanced). Variation between blocks is
 * handled via props (preset lists, field maps, item-type labels) — the
 * components themselves stay identical.
 */
export { AdvancedStyling } from './AdvancedStyling';
export { AnimationControls } from './AnimationControls';
